server:
  host: "0.0.0.0"
  port: 8001
  mode: release

service:
  name: user-service

mysql:
  host: mysql
  port: 3306
  user: ash
  password: ash_dev_password
  database: ash_dev
  charset: utf8mb4
  max_idle_conns: 5
  max_open_conns: 20

log:
  level: info
  format: json
