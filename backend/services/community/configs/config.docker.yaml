server:
  host: "0.0.0.0"
  port: 8003
  mode: release

service:
  name: community-service

mysql:
  host: mysql
  port: 3306
  user: ash
  password: ash_dev_password
  database: ash_dev
  charset: utf8mb4
  max_idle_conns: 5
  max_open_conns: 20

auth:
  jwt_secret: ash_dev_jwt_secret_change_me
  access_cookie_name: ash_at
  access_ttl_minutes: 15

log:
  level: info
  format: json
