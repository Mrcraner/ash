server:
  host: "0.0.0.0"
  port: 8003
  mode: release

service:
  name: community-service

mysql:
  host: mysql
  port: 3306
  user: ash
  password: change_me_test_ash
  database: ash_test
  charset: utf8mb4
  max_idle_conns: 3
  max_open_conns: 10

auth:
  jwt_secret: ash_dev_jwt_secret_change_me
  access_cookie_name: ash_at
  access_ttl_minutes: 15

log:
  level: debug
  format: json
